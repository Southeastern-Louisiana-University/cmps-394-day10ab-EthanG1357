events {}

http {
    upstream api_backend {
        server api1:8000;
        server api2:8000;
    }

    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        location /api/ {
            proxy_pass http://api_backend/;
        }
    }
}